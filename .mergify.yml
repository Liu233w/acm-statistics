rules:
  default:
    protection:
      required_status_checks:
        strict: false
        contexts:
          - continuous-integration/travis-ci
          - codecov/project
          - WIP
      required_pul
    merge_strategy:
      method: rebase
    enabling_label: ready-to-be-merged
