# == docker compose 参数 ==

# 监听的端口
EXPOSE_PORT=3000
# traefik 的监听端口，不建议暴露给外部
TRAEFIK_PORT=3001

# 镜像名和标签，不需要修改
DOCKER_REPO=
DOCKER_TAG=

# == 内部程序的参数 ==
# vjudge 爬虫用到的，填你自己的即可
VJUDGE_USERNAME=vjudge用户名
VJUDGE_PASSWORD=vjudge密码
